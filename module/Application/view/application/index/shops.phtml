<?php
$title = 'Магазины';
$this->headTitle($title);
?>
<div id="content">
    <div class="center">               
        <div class="twoCol">
            <div class="shop_block" >
                <div class="leftCol raised">
                    <?php
                    $partial = array('partial/category_menu.phtml', 'default');
                    echo $this->navigation('cat_navigation')
                            ->menu()
                            ->setMinDepth(1)
                            ->setMaxDepth(4)
                            ->setRenderParents(false)
                            ->setOnlyActiveBranch(true)
                            ->setPartial($partial)
                            ->render();
                    ?>
                </div>
                <div class="rightCol">
                    <div class="title">
                        <h1><?php echo $this->escapeHtml($title); ?></h1>
                    </div>
                    <ul class="shopslist">
                        <?php foreach ($this->paginator as $shop) : ?>
                        <?php $patchs = explode(',', $shop['patch']);
                        $address = explode(',', $shop['address']);
                        ?>
                        
                            <li>
                                <a href="<?php echo $this->url('shops', array('action' => 'shop', 'id' => $shop['id'])); ?>" title="<?php echo $address[0] . ',' . $address[1] ?>">
                                   <img src ="<?php echo '/images/shop/small_' . trim($patchs[0]) ?>" alt="<?php echo $shop['address'] ?>"  >
                                </a>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                    <br class="clear">

                </div>
            </div>
        </div>
    </div>
</div>
