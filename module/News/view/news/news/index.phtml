<?php
$title = 'Новости';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>
<p>
    <a href="<?php echo $this->url('news', array('action' => 'add')); ?>">
        Добавить новость</a>
</p>
<p>
    <a href="<?php echo $this->url('news', array('action' => 'index','status'=>1)); ?>">
       Показать неопубликованные</a>
</p>
<table class="table">
    <tr>
        <th>Название</th>
        <th>Описание</th>
        <th>Статус</th> 
        <th>&nbsp;</th>
    </tr>
    <?php foreach ($this->paginator as $reviews) : ?>
        <tr>
            <td><?php echo $this->escapeHtml($reviews->name); ?></td>
            <td><?php echo $this->escapeHtml($reviews->description); ?></td>
            <td><?php echo $this->escapeHtml($reviews->status) ? 'опублекован' :'не обубликован' ?></td>
            <td>
                <a href="<?php echo $this->url('news', array('action' => 'edit', 'id' => $reviews->id));
        ?>">Редактировать</a>
                <a href="<?php echo $this->url('news', array('action' => 'delete', 'id' => $reviews->id));
        ?>">Удалить</a>
            </td>
        </tr>
    <?php endforeach; ?>
</table>
<?php
echo $this->paginationControl(
// the paginator object
        $this->paginator,
// the scrolling style
        'sliding',
// the partial to use to render the control
        array('partial/paginator.phtml', 'News'),
// the route to link to when a user clicks a control link
        array('route' => 'news'
        )
);
